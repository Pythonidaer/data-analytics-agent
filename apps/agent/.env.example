# Environment Variables for Data Analytics Agent
# Copy this file to .env and fill in your values
# The .env file is git-ignored and will not be committed

# ============================================
# REQUIRED
# ============================================

# Your Anthropic API key for Claude API access
# Get it from: https://console.anthropic.com
# Format: sk-ant-api03-xxxxxxxxxxxxx
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ============================================
# DOMAIN SELECTION (Required for Analytics Agent)
# ============================================

# Select which domain agent to use
# Options:
#   - "pmm" (default) - Product Marketing Manager agent
#   - "data_analytics" - Data Analytics Expert Agent
# 
# Set to "data_analytics" to use the Analytics Expert Agent
DOMAIN=data_analytics

# ============================================
# OPTIONAL - Model Configuration
# ============================================

# Claude model to use
# Options:
#   - claude-sonnet-4-20250514 (default, most capable, ~$3/$15 per 1M tokens)
#   - claude-3-5-haiku-20241022 (cheaper, faster, ~$0.25/$1.25 per 1M tokens)
#   - claude-3-5-sonnet-20241022 (middle ground)
#
# Note: Sonnet 4 is recommended for better instruction following (e.g., showing full SQL output)
# Haiku is cheaper but may summarize tool outputs instead of showing them verbatim
MODEL=claude-sonnet-4-20250514

# ============================================
# OPTIONAL - Server Configuration
# ============================================

# Maximum number of messages to keep in conversation history (prevents token bloat)
# Default: 100
MAX_MESSAGE_HISTORY=100

# Logging verbosity level
# Options: DEBUG, INFO (default), WARNING, ERROR
LOG_LEVEL=INFO

# Comma-separated list of allowed CORS origins
# In development, defaults to "*" (all origins)
# In production, should be set to your specific domain(s)
# Example: ALLOWED_ORIGINS=https://your-app.vercel.app,https://www.yourdomain.com
# Leave empty for development (defaults to "*")
ALLOWED_ORIGINS=

# ============================================
# PRODUCTION (Auto-set by Vercel)
# ============================================

# These are automatically set by Vercel, don't set them manually
# VERCEL=1
# PRODUCTION=true
# ENVIRONMENT=production

# ============================================
# QUICK START
# ============================================

# Minimum required for local development:
# 1. ANTHROPIC_API_KEY (required)
# 2. DOMAIN=data_analytics (required for analytics agent)

# Example minimal .env file:
# ANTHROPIC_API_KEY=sk-ant-your-key-here
# DOMAIN=data_analytics
